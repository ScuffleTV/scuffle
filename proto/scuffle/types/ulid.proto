syntax = "proto3";

package scuffle.types;

// ULID is a 128-bit universally unique identifier that is lexicographically
// sortable and contains a timestamp. See
message Ulid {
  uint64 most_significant_ulid_bits = 1;
  uint64 least_significant_ulid_bits = 2;
}
